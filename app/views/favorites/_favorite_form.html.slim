- if @favorite.present?
  = link_to 'お気に入り解除', favorite_path(id: @favorite, review_id: @review), method: :delete, remote: true, class: 'btn btn-danger'
- else
  = link_to 'お気に入り', favorites_path(review_id: @review), method: :post, remote: true, class: 'btn btn-primary'
